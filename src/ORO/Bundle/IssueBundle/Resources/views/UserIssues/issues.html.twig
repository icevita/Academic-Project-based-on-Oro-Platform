{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set widgetContent %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('user_issues', { userId: entity.id }),
        'title': 'oro.issue.user_issues.label'|trans
    }) }}
{% endset %}
{{ UI.scrollSubblock(null, [widgetContent]) }}
